
export default class User{
    constructor(public idFamily:string, public firstName:string, public lastName:string, public id:string,
        public birthDate:Date, public sex:number ,public HMO:number, public status:number){
        
    }
}